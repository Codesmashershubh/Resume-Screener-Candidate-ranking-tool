# Copy to .env and load with `export $(cat .env | xargs)` or a tool like
# python-dotenv if you want these outside of Render's dashboard env vars.
PORT=8000
ALLOWED_ORIGINS=http://localhost:5173,http://127.0.0.1:5173
ENABLE_HISTORY=true
MAX_STORED_SESSIONS=50
