# Copy to .env.local for local development.
# Points the frontend at your local backend by default; api.ts already
# falls back to http://localhost:8000 if this isn't set at all.
VITE_API_URL=http://localhost:8000
